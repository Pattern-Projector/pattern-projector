import { useState } from "react";

function useProgArrowKeyHandler() {
  const [pixelGap, setPixelGap] = useState<number>(1);

  function keydownHandler() {}

  if (typeof window !== "undefined") {
  }
}
